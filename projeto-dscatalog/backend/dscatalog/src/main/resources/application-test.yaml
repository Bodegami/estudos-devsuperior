# H2 connection
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:

# H2 client
  h2:
    console:
      enabled: true
      path: /h2-console

# Show sql
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true